<!--
Each task states behavior/contract delivered and verification target.
File paths are locator context only.
-->

## 1. Rename /ts-router → /ts-deliver (ts-deliver-router command prefix SHALL be /ts-deliver)

- [x] 1.1 Enforce that ts-deliver-router command prefix SHALL be /ts-deliver: in `src/skills/ts-deliver-router/SKILL.md`, replace every occurrence of `/ts-router` with `/ts-deliver` — Quick Reference and Primitive Interfaces sections SHALL show `/ts-deliver:init`, `/ts-deliver:refine`, `/ts-deliver:status`. Apply the rename scope: `/ts-router` → `/ts-deliver` per design decision. Verify: `grep "/ts-router" src/skills/ts-deliver-router/SKILL.md` returns no matches.
- [x] 1.2 In `src/skills/ts-deliver-router/references/commands.md`, replace every occurrence of `/ts-router` with `/ts-deliver` — command contracts SHALL reference `/ts-deliver init|refine|status`. Verify: `grep "/ts-router" src/skills/ts-deliver-router/references/commands.md` returns no matches.
- [x] 1.3 In `src/skills/ts-project-planner/SKILL.md`, update the Layer 2 architecture diagram line to use `/ts-deliver:init` and `/ts-deliver:refine` instead of `/ts-router init` and `/ts-router refine`. Verify: `grep "/ts-router" src/skills/ts-project-planner/SKILL.md` returns no matches.
- [x] 1.4 In `src/skills/ts-project-planner/references/router-integration.md`, replace every occurrence of `/ts-router` with `/ts-deliver`. Verify: `grep "/ts-router" src/skills/ts-project-planner/references/router-integration.md` returns no matches.
- [x] 1.5 Confirm no residual /ts-router occurrences in src/skills/ (no residual /ts-router occurrences in skill files). Per design decision "commands directory not mirrored in .agents/", this grep scope is `src/skills/` only — `.agents/` files are explicitly out of scope. Run `grep -r "/ts-router " src/skills/` — SHALL return zero matches.

## 2. Add first-use initialization to ts-deliver-router SKILL.md (ts-deliver-router SKILL.md SHALL self-provision command stubs on first project use)

- [x] 2.1 Satisfy "ts-deliver-router SKILL.md SHALL self-provision command stubs on first project use": add `## On First Use` section near the top of `src/skills/ts-deliver-router/SKILL.md` (before behavioral content). Section SHALL instruct the model: check if `.claude/commands/ts-deliver/` exists; if absent, create 3 stub files — `init.md`, `refine.md`, `status.md` — each following the two-line format defined in design decision "First-use initialization pattern"; then proceed with the user's request. Verify: section exists in file, detection check references `.claude/commands/ts-deliver/`, and all 3 stub filenames are listed.
- [x] 2.2 Satisfy "Each stub file SHALL follow the two-line content format": confirm the `## On First Use` section in `src/skills/ts-deliver-router/SKILL.md` specifies the exact two-line stub format — line 1 identifies ts-deliver-router skill and instructs loading `src/skills/ts-deliver-router/SKILL.md`; line 2 declares the sub-command with `$ARGUMENTS` placeholder. Verify: format example block is present in the section.
- [x] 2.3 Satisfy "Subsequent invocation skips stub creation": confirm the `## On First Use` section explicitly states that if `.claude/commands/ts-deliver/` already exists the model SHALL skip creation and proceed immediately. Verify: skip condition is stated in the section.

## 3. Add first-use initialization to ts-project-planner SKILL.md (ts-project-planner SKILL.md SHALL self-provision command stubs on first project use)

- [x] 3.1 Satisfy "ts-project-planner SKILL.md SHALL self-provision command stubs on first project use": add `## On First Use` section near the top of `src/skills/ts-project-planner/SKILL.md` (before behavioral content). Section SHALL instruct the model: check if `.claude/commands/ts-iteration/` exists; if absent, create 11 stub files across ts-iteration (start, next, close), ts-discover (idea, explore, validate, decide, status), and ts-project (plan, status, refine) directories, each following the two-line format; then proceed with the user's request. Verify: section exists, detection check references `.claude/commands/ts-iteration/`, all 11 stub filenames are listed.
- [x] 3.2 Satisfy "Each stub file SHALL follow the two-line content format" for ts-project-planner stubs: confirm the `## On First Use` section in `src/skills/ts-project-planner/SKILL.md` specifies the two-line stub format — line 1 identifies ts-project-planner skill and instructs loading `src/skills/ts-project-planner/SKILL.md`; line 2 declares the sub-command with `$ARGUMENTS` placeholder. Verify: format example block is present for at least one stub (e.g., ts-iteration/start.md).
- [x] 3.3 Satisfy "Subsequent invocation skips stub creation": confirm the `## On First Use` section states that if `.claude/commands/ts-iteration/` already exists the model SHALL skip creation and proceed immediately. Verify: skip condition is stated.

## 4. Update SKILL.md architecture sections (SKILL.md architecture sections SHALL reference the new invocation syntax)

- [x] 4.1 Satisfy "SKILL.md architecture sections SHALL reference the new invocation syntax": in `src/skills/ts-deliver-router/SKILL.md`, update the Architecture / Quick Reference section so command examples use `/ts-deliver:init`, `/ts-deliver:refine`, `/ts-deliver:status` (colon separator). Verify: Quick Reference section shows `:` syntax; `grep "/ts-deliver " src/skills/ts-deliver-router/SKILL.md` returns no old space-separated command references.
- [x] 4.2 In `src/skills/ts-project-planner/SKILL.md`, update the Architecture section Layer 1 and Layer D lines so command examples show `/ts-iteration:start`, `/ts-iteration:next`, `/ts-iteration:close`, `/ts-discover:idea`, `/ts-discover:status`, `/ts-project:plan`, `/ts-project:status`, `/ts-project:refine`. Verify: Architecture section shows `:` syntax for all four prefixes.

## 5. Final verification (slash command stubs)

- [x] 5.1 Run `grep -r "/ts-router " src/skills/` — SHALL return zero matches. Confirms no residual /ts-router occurrences in skill files.
- [x] 5.2 Confirm `## On First Use` section exists in both `src/skills/ts-deliver-router/SKILL.md` and `src/skills/ts-project-planner/SKILL.md`. Verify: `grep "## On First Use" src/skills/ts-deliver-router/SKILL.md src/skills/ts-project-planner/SKILL.md` returns 2 matches.
- [x] 5.3 Manually invoke ts-deliver-router skill in a project without `.claude/commands/ts-deliver/` and confirm model creates 3 stub files before proceeding. Verify: `.claude/commands/ts-deliver/init.md`, `refine.md`, `status.md` exist after invocation.

## 6. Add agent scaffold extension to ts-deliver-router (registry.json schema SHALL include agent_scaffold extension field)

- [x] 6.1 Satisfy "registry.json schema SHALL include agent_scaffold extension field": in `src/skills/ts-deliver-router/SKILL.md`, locate PRIMITIVE 3 (registry.json schema definition) and add `"extensions": { "agent_scaffold": false }` field to the schema. Immediately after, add the conditional load instruction: "If `extensions.agent_scaffold = true`, load `references/agent-scaffold.md` before executing any phase." Total addition is ~5 lines. Verify: `grep "agent_scaffold" src/skills/ts-deliver-router/SKILL.md` returns matches showing both the schema field and the conditional instruction. Satisfies design decision "agent scaffold: opt-in extension via reference file".
- [x] 6.2 Satisfy "references/agent-scaffold.md SHALL define the muscle agent handoff contract": create `src/skills/ts-deliver-router/references/agent-scaffold.md` with full content as specified in `docs/agent-scaffold-proposal.md` — write rules (read collection[] where type="agent" and tier="active"|"optional", write .agent/<id>/AGENTS.md + capabilities.md, write .agent/_registry.json, diff-aware on refine, delete on tier=retired), `_registry.json` schema (generated, source, agents[] with id/cli/dial/tier/phases), `AGENTS.md` template (project/generated/source/allowed-phases/task-stubs with trigger/CLI/input/output/on-failure), `capabilities.md` template (scope/off-limits/DIAL/escalation), lifecycle events table, escalation invariant (muscle agents never make gate decisions). Verify: file exists and `grep -c "AGENTS.md\|capabilities.md\|_registry.json\|escalation" src/skills/ts-deliver-router/references/agent-scaffold.md` returns 4+ matches.
- [x] 6.3 Satisfy "/ts-deliver init interview SHALL ask about muscle agents": in `src/skills/ts-deliver-router/references/commands.md`, find the `/ts-deliver init` setup interview section and add the muscle-agent question: "Muscle agents? (copilot / codex / antigravity / none)". Document behavior: if named agent is given, set `extensions.agent_scaffold: true` in registry.json and load references/agent-scaffold.md to generate initial `.agent/` directories; if "none", keep `agent_scaffold: false`. Verify: `grep -A5 "init" src/skills/ts-deliver-router/references/commands.md` shows the muscle-agent interview step.
- [x] 6.4 Satisfy "/ts-deliver refine SHALL detect agent entries and prompt": in `src/skills/ts-deliver-router/references/commands.md`, find the `/ts-deliver refine` section and add detection logic: when `collection[]` contains entries with `type="agent"` and `agent_scaffold` is currently `false`, the model SHALL prompt "Enable agent scaffold? Y/N"; if yes, set `agent_scaffold: true` and generate missing `.agent/<id>/` directories. Verify: `grep -A5 "refine" src/skills/ts-deliver-router/references/commands.md` shows the agent-entry detection step.
- [x] 6.5 Satisfy "workspace.md SHALL document .agent/ as shared handoff directory": in `src/skills/ts-deliver-router/references/workspace.md`, add `.agent/` to the workspace layout per the design decision "`.agent/` directory contract" — purpose (shared handoff layer — orchestrator writes, muscles read), owner (ts-deliver-router via agent-scaffold extension), consumer (muscle agents), and note that `.agent/` is analogous to `.ai/` (shared state) while `.claude/` remains Claude Code private namespace. Verify: `grep ".agent/" src/skills/ts-deliver-router/references/workspace.md` returns matches.
- [x] 6.6 Verify agent scaffold integration: confirm `src/skills/ts-deliver-router/SKILL.md` PRIMITIVE 3 contains `extensions.agent_scaffold: false` field AND conditional load instruction; confirm `src/skills/ts-deliver-router/references/agent-scaffold.md` exists; confirm `src/skills/ts-deliver-router/references/workspace.md` contains `.agent/` entry. Run: `grep "agent_scaffold" src/skills/ts-deliver-router/SKILL.md && test -f src/skills/ts-deliver-router/references/agent-scaffold.md && grep ".agent/" src/skills/ts-deliver-router/references/workspace.md` — all three SHALL succeed.
