## 1. Verification harness: byte-diff against a captured baseline

- [x] 1.1 Capture pre-refactor baseline: run `npm run release`, `npm run pilot`, `npm run dogfood`, `node scripts/verify-install.mjs`; save each command's full stdout AND output/artifacts (zip file listing, pilot install tree, dogfood `.claude/` tree, verify-install result) to a scratch dir for later diffing. Verified by: all 4 commands exit 0, baseline artifacts exist on disk, and each command's captured stdout is non-empty.

## 2. dogfood.mjs: extract pushCategoryTargets

- [x] 2.1 Implement `pushCategoryTargets(targets, entries, {srcRoot, destRoot, dir})` in `scripts/dogfood.mjs`, replacing the 3 push loops in `mirrorTargets()` with 3 calls (skills: `dir: true`, hooks/agents: `dir: false`). Verified by: `mirrorTargets()` returns the same array shape as before (manual diff of a captured JSON dump of `mirrorTargets()` pre/post).
- [x] 2.2 Replace the 3 push loops in the `--from-zip` branch with 3 calls to `pushCategoryTargets`, sourced from the extracted `zipManifest`. Verified by: `npm run dogfood -- --from-zip <test-zip>` produces an identical mirror to pre-refactor behavior on the same test zip.
- [x] 2.3 Add a Vitest unit test (`scripts/tests/dedup-helpers.test.mjs`) asserting `pushCategoryTargets` produces the exact `{src, dest, dir, zipPath}` shape the current inline loops produce today, given a fixture manifest with 1 skill + 1 hook + 1 agent entry (dir:true for skills, dir:false for hooks/agents). Verified by: `npx vitest run scripts/tests/dedup-helpers.test.mjs` passes.

## 3. build-release.mjs: extract copyManifestCategory

- [x] 3.1 Implement `copyManifestCategory(entries, {rootDir, buildDir, filtered, validate, label})` in `scripts/build-release.mjs`; replace the skills loop with a call passing `validate: validateSkill, filtered: true`. Verified by: `npm run release` still throws when a skill directory is missing `SKILL.md` (existing `validateSkill` behavior preserved).
- [x] 3.2 Replace the hooks and agents loops with calls to `copyManifestCategory` omitting `validate`/`filtered`. Verified by: `dist/release.zip` file listing after `npm run release` is byte-identical to the Task 1.1 baseline listing.
- [x] 3.3 Add a Vitest unit test (`scripts/tests/dedup-helpers.test.mjs`) asserting `copyManifestCategory` calls the injected `cp`/`validate` stubs for each entry with the right `{filtered}` flag (true for skills, false/omitted for hooks/agents), invokes `validate` only when passed (skills), and reproduces the pre-refactor asymmetry exactly: skills-style (validate passed) checks for a missing source up front and warns with the relative `src` path, skipping `validate`/`cp` entirely; hooks/agents-style (validate omitted) always calls `cp` (which owns its own missing-source skip+warn) and only suppresses the success log line if the source is missing. Use small real fixture files in a `mkdtempSync` temp dir for the `existsSync` checks (fast, isolated from the project tree) — `cp`/`validate` themselves are `vi.fn()` stubs, so nothing is ever copied for real. Verified by: `npx vitest run scripts/tests/dedup-helpers.test.mjs` passes.

## 4. install.sh: consolidate to one tagged loop

- [x] 4.1 Replace the 3 `python3 -c` subprocess spawns with one heredoc that emits all rows across skills/hooks/agents (skill rows first, then hook rows, then agent rows — same relative order as today's 3 loops), each line tagged (`skill\t...`/`hook\t...`/`agent\t...`). Move the 3 `→ Installing skills/hooks/agents...` header echoes to print together, up front, right after the mkdir block, in that same order — a deliberate, user-confirmed stdout reordering (same text, grouped instead of interleaved-per-category; avoids a per-tag header-shown flag that would silently vanish for an empty category). Verified by: `bash -n release/install.sh` (syntax check) passes.
- [x] 4.2 Keep the existing `mkdir -p "${SKILLS_DIR}"`, `mkdir -p "${HOOKS_DIR}"`, `mkdir -p "${PROJECT_HOOKS_DIR}"`, and `mkdir -p "${PROJECT_CLAUDE_DIR}/agents"` calls all running once, up front, before the merged loop starts (not per-row, not per-category-inside-the-loop) — a fresh install with no pre-existing `.claude/` subdirs must not break on the first `cp`. Replace the 3 separate bash `while read` loops with one `while IFS=$'\t' read -r tag f1 f2 f3` loop (4 variables — max field-width across categories, since hook rows carry 3 data fields vs skill/agent's 2) dispatching via `case "$tag" in skill) ...;; hook) ...;; agent) ...;; *) echo "unknown manifest category tag: $tag" >&2; exit 1 ;; esac` (the `*)` arm is new — today's 3 separate loops have no equivalent because there's no dispatch to get wrong; the merged loop must fail loudly on an unrecognized tag rather than silently dropping the row), each named arm performing the exact current per-category action (skills: `mkdir -p` + `cp -r` using `f1`/`f2`; hooks: scope-routed `cp` + `chmod +x` using `f1`/`f2`/`f3`; agents: `cp` renamed to `<name>.md` using `f1`/`f2`) and ignoring any trailing empty variables. Using fewer than 4 read variables would silently corrupt the last field of the widest row (bash `read` folds excess columns, with their original tab separators, into the final declared variable) — this must not happen. Verified by: `node scripts/verify-install.mjs` passes (sh/ps1 parity check covers this file's output), plus a manual test feeding a fixture row with a bogus tag confirms the script exits non-zero with the error message instead of silently continuing.

## 5. install.ps1: mirror the tagged-loop shape

- [x] 5.1 Keep the existing `New-Item -ItemType Directory -Path $HooksDir/$ProjectHooksDir/$AgentsDir -Force` calls all running once, up front, before the merged loop starts (not per-row). Define `$InstallActions` hashtable of scriptblocks (`skill`, `hook`, `agent`), each scriptblock body identical to today's corresponding foreach body, and replace the 3 foreach blocks with one `foreach` that checks `$InstallActions.ContainsKey($tag)` before invoking — `if (-not $InstallActions.ContainsKey($tag)) { throw "unknown manifest category tag: $tag" }` — then calls `$InstallActions[$tag].Invoke($entry)` over a category-tagged flat list built from `$manifest.skills`/`.hooks`/`.agents` (the explicit key check is new — today's 3 separate foreach blocks have no equivalent lookup to fail; an unguarded hashtable miss would throw an opaque null-ref instead of a clear error). Verified by: `pwsh -NoProfile -Command "[void][System.Management.Automation.PSParser]::Tokenize((Get-Content release/install.ps1 -Raw), [ref]$null)"` (real static syntax check — NOT `-WhatIf`, which this script doesn't support via `param()`/`CmdletBinding`/`SupportsShouldProcess` and would just execute the full installer for real) passes; live execution is deferred entirely to task 6.2's manual smoke test.

## 6. Full regression verification

- [x] 6.1 Re-run `npm run release`, `npm run pilot`, `npm run dogfood`, `node scripts/verify-install.mjs`; diff each result against the Task 1.1 baseline (zip listing diff, `diff -r` pilot install tree, `diff -r` dogfood `.claude/` tree, AND a plain-text diff of each command's captured stdout against its Task 1.1 baseline stdout). Note: `pilot.mjs` runs install.sh via `spawnSync` without `stdio: 'inherit'` and only surfaces `install.stdout`/`install.stderr` on a non-zero exit — install.sh's own echo/header output is not visible in pilot's captured stdout on success, so the task 4.1 header reorder does NOT show up in this diff; it was independently confirmed by running install.sh directly against a fixture (see review/report.md). Verified by: all 4 diffs empty (pilot's stdout diff only reflects the random per-run fixture tmpdir path, which is expected noise, not a regression), all 4 commands exit 0.
- [x] 6.2 Manual smoke: run `install.ps1` under the local `pwsh 7.6.3` against a locally-built zip (`$env:ZIP_FILE`), inspect the resulting tree matches the sh-side baseline. Verified by: visual tree comparison shows no discrepancy (documented in review/report.md).
