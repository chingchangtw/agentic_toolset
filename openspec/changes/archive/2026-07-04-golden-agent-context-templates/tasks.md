## 1. Sync module and script

- [x] 1.1 Implement scripts/lib/golden-templates.mjs exporting GOLDEN_FILES (array of the 5 relative subpaths: CLAUDE.md, AGENTS.md, .claude/CLAUDE.md, .claude/goverance_CLAUDE.md, .github/copilot-instructions.md), satisfying requirement "Sync copies golden files preserving subpaths" and design decision "sync and parity-check share one comparison function" + "dotted subpaths are preserved, not flattened": syncGoldenTemplates() copies each src/project_root_structure/<relpath> to src/skills/ondemand/ts-project-init-advisor/assets/golden/<relpath>, preserving dotted subdirectories, overwriting unconditionally. Verify: unit test asserting assets/golden/.claude/CLAUDE.md is byte-identical to src/project_root_structure/.claude/CLAUDE.md after calling syncGoldenTemplates() against a temp fixture tree.
- [x] 1.2 Add npm run sync-golden script (scripts/sync-golden-templates.mjs calling syncGoldenTemplates()) satisfying requirement "npm run sync-golden invokes the sync" and design decision "assets/golden/ is git-tracked, not generated fresh on every build" (updated via developer-run sync, not regenerated at build time). Verify: run npm run sync-golden against the real src/project_root_structure/, confirm all 5 files land under src/skills/ondemand/ts-project-init-advisor/assets/golden/ with correct subpaths via ls -R.

## 2. Parity gate

- [x] 2.1 Implement checkGoldenParity() in scripts/lib/golden-templates.mjs, satisfying requirement "checkGoldenParity reports every drifted or missing golden file" and design decision "sync and parity-check share one comparison function": returns [] when all 5 files match; returns {file, reason:"missing"} for absent assets/golden/ copies; returns {file, reason:"content-mismatch"} for byte-differing copies. Verify: unit tests covering all three scenarios from the golden-template-parity-gate spec (single-character drift, missing file, fully-synced-returns-empty) pass via npm test -- tests/unit/golden-templates.test.ts.
- [x] 2.2 Wire checkGoldenParity() into scripts/build-release.mjs before the zip-creation step, satisfying requirement "Release build fails on any parity drift": prints "golden template drift: <file> (<reason>)" per entry and exits non-zero without writing dist/release.zip when the array is non-empty. Verify: temporarily edit one character in an assets/golden/ file without re-running sync, confirm node scripts/build-release.mjs exits non-zero with the exact drift line naming that file, then revert and confirm a clean build exits zero.

## 3. Advisor gap-scan and task emission

- [x] 3.1 Add a golden-file gap-scan step to src/skills/ondemand/ts-project-init-advisor/SKILL.md (Phase 3/4, before PROJECT_INIT_PLAN.md generation), satisfying requirement "Advisor creates absent golden files directly": for each of the 5 golden files, check the target project's corresponding path; if absent, add a create task using the packaged assets/golden/<relpath> content, no approval gate. Verify: manually run the advisor against a scratch project directory with none of the 5 files present, confirm the generated PROJECT_INIT_PLAN.md contains 5 create tasks and executing them produces files byte-identical to assets/golden/.
- [x] 3.2 Extend the same SKILL.md gap-scan step to satisfy requirement "Advisor never writes to an existing golden-file target" and "Diverging existing files get a diff-only MODIFY task", per design decision "modify task content is a mechanical diff, never a semantic merge decision": if a golden file's target exists and differs, emit a MODIFY task containing the target path and an agent-produced comparison (the advisor reads both files directly and describes the diff itself — no diff library, no new dependency, per design.md D4's diff generation mechanism note) between target and golden content, with no pre-computed merge and no direct write. Verify: manually run the advisor against a scratch project with one deliberately modified AGENTS.md, confirm the generated plan contains exactly one MODIFY task for AGENTS.md describing the specific content difference, and confirm no file on disk was altered by the advisor run itself.
- [x] 3.3 Extend the gap-scan step to satisfy requirement "Byte-identical existing files produce no task": if a golden file's target exists and matches byte-for-byte, no task is emitted for it. Verify: manually run the advisor against a scratch project where all 5 golden files are already byte-identical copies, confirm the generated PROJECT_INIT_PLAN.md contains zero tasks referencing any of the 5 golden filenames.

## 4. Coexistence documentation

- [x] 4.1 Document the design decision "scaffold=y coexistence posture" in src/skills/ondemand/ts-project-init-advisor/SKILL.md: install.sh's SCAFFOLD=y is unchanged, copies the full src/project_root_structure/ tree at install time, and is idempotent with the advisor's gap-scan (a SCAFFOLD=y project naturally lands in the present-and-matching, no-task branch). Verify: grep for "SCAFFOLD" in src/skills/ondemand/ts-project-init-advisor/SKILL.md returns at least one match, and manual read confirms the posture is stated in prose, not just a file reference.

## 5. Regression check

- [x] 5.1 Confirm the full pipeline works end to end: npm run sync-golden, npm run type-check, npm test, node scripts/build-release.mjs, and skill-validator check src/skills/ondemand/ts-project-init-advisor/ all exit zero (skill-validator errors specifically, warnings acceptable), and unzip -l dist/release.zip lists all 5 golden files under skills/ondemand/ts-project-init-advisor/assets/golden/ with correct subpaths. Verify: command exit codes and zip listing.
