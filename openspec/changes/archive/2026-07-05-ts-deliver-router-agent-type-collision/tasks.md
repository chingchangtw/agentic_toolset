## 1. Schema and reference file updates

- [x] 1.1 Split `type="agent"` into `type="muscle-agent"` and `type="subagent"` in `registry-schema.md`'s `collection[].type` enum table (list `skill` / `plugin` / `mcp` / `muscle-agent` / `subagent`, remove the bare `agent` value), satisfying "registry.json collection[].type SHALL distinguish muscle-agent from subagent". Verify: `grep -n 'type.*agent' src/skills/ts-deliver-router/references/registry-schema.md` shows only `muscle-agent`/`subagent` occurrences, zero bare `agent` matches.
- [x] 1.2 Apply `registry-schema.md`'s reference example corrected fix: update the example `registry.json`/`registry.log` so any sub-agent entry (e.g. `ts-event-storming-facilitator`) shows `"type": "subagent"`, satisfying "Reference example reflects the split". Verify: manual read of the example block confirms `"type": "subagent"` appears on the `ts-event-storming-facilitator` line.
- [x] 1.3 Apply `/ts-deliver refine`'s scaffold-detection trigger scoped to `type="muscle-agent"`: update `commands.md`'s trigger text from `type="agent"` to `type="muscle-agent"`, satisfying "/ts-deliver refine SHALL detect agent entries and prompt" (now scoped to muscle-agent). Verify: `grep -n 'type="agent"' src/skills/ts-deliver-router/references/commands.md` returns zero matches; `grep -n 'type="muscle-agent"'` returns at least one.
- [x] 1.4 Apply `agent-scaffold.md` write rules filter on `type="muscle-agent"` only: update write-rules step 1 from `type = "agent"` to `type = "muscle-agent"`, satisfying "references/agent-scaffold.md SHALL define the muscle agent handoff contract" (modified) and satisfying that type="subagent" entries SHALL NOT trigger agent-scaffold.md. Verify: `grep -n 'type = "agent"' src/skills/ts-deliver-router/references/agent-scaffold.md` returns zero matches; `grep -n 'type = "muscle-agent"'` returns at least one.

- [x] 1.5 Update `openspec/specs/ts-deliver-router-agent-scaffold/spec.md`'s three `type="agent"`-referencing requirements to `type="muscle-agent"`, and add a new requirement documenting that `type="subagent"` entries never trigger `agent-scaffold.md`'s write path (built via `sub-agents.md` instead, no automated detection). Verify: `grep -n 'type="agent"\|type = "agent"' openspec/specs/ts-deliver-router-agent-scaffold/spec.md` returns zero matches; `grep -n 'subagent' openspec/specs/ts-deliver-router-agent-scaffold/spec.md` returns at least one match including the new requirement.

## 2. Registry migration

- [x] 2.1 Migrate `.agents/ts-deliver-router/registry.json`'s 4 sub-agent entries (`ts-event-storming-facilitator`, `ts-spec-validator`, `ts-ddd-tactical-validator`, `ts-mutation-analyst`) from `"type": "agent"` to `"type": "subagent"`, so these `type="subagent"` entries SHALL NOT trigger agent-scaffold.md going forward. Verify: `grep -n '"type": "agent"' .agents/ts-deliver-router/registry.json` returns zero matches; `node -e "JSON.parse(require('fs').readFileSync('.agents/ts-deliver-router/registry.json','utf8'))"` confirms valid JSON.

## 3. Regression check

- [x] 3.1 Confirm no other reference file in `src/skills/ts-deliver-router/references/` still uses the bare `type="agent"` or `type = "agent"` filter after tasks 1.1-1.4, satisfying the overall requirement that the collision is fully closed. Verify: `grep -rn 'type="agent"\|type = "agent"' src/skills/ts-deliver-router/references/` returns zero matches across the whole directory.
- [x] 3.2 Resync this repo's own dogfooded skill mirror by running `npm run dogfood`, so the fix takes effect for this project's own live skill usage (not just future packaged installs) — `.claude/skills/ts-deliver-router/references/` is a gitignored mirror of `src/skills/ts-deliver-router/references/` and independently carries the same stale `type="agent"` text until resynced. Verify: `grep -rn 'type="agent"\|type = "agent"' .claude/skills/ts-deliver-router/references/` returns zero matches after the sync.
