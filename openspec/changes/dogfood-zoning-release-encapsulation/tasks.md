## 1. Release pipeline hardening

- [ ] 1.1 Create `scripts/lib/exclusions.mjs` exporting the shared packaging exclusion filter (directory names `rawfiles`, `raw`, `ideas`, `registry`, `node_modules`; file patterns `*.original.md`, `SKILL_caveman.md`, `README.md`, `.DS_Store`) as a predicate usable as a `cpSync` filter. Implements design decision "Packaging uses a global exclusion filter shared by build and sync" and spec requirement "Shared packaging exclusion filter". Verify: check via `node -e` that `rawfiles/x.md` is excluded and `SKILL.md` is included.
- [ ] 1.2 Modify `scripts/build-release.mjs` per design decisions "Manifest is the single contract driving sync, gitignore, and packaging" and "Version stamping via manifest field plus installed marker": invoke the manifest generator before packaging (spec "Build always regenerates the manifest"), apply the exclusion filter to every skill copy, stamp `releaseVersion` from `package.json` into the zip-root `manifest.json` (spec "Zip manifest carries stamped release version"), and remove `.release-build/` after the zip is written (spec "Build staging cleanup"). Verify: build with a planted `rawfiles/` file → absent from zip; unzipped `manifest.json` has `releaseVersion` matching `package.json` and `version` `"1"`; `.release-build/` gone.
- [ ] 1.3 Delete `src/hook/ts-statusline_wrapper.sh` and `src/hook/ts-statusline_wrapper.ps1`, then regenerate the manifest — spec requirement "Dead wrapper hooks removed from deliverables", design decision "Remove dead packaged wrappers, install commands". Verify: `grep -c statusline_wrapper scripts/release-manifest.json` is 0 and no wrapper file in the built zip.
- [ ] 1.4 Modify `release/install.sh` and `release/install.ps1` per spec requirement "Installers deploy commands and version marker": deploy the zip's `commands/` directory to the target project's `.claude/commands/`, and write `releaseVersion` from `manifest.json` to `.claude/.toolset-version` (skip marker silently when the field is absent — legacy zip path must still succeed). Add a `ZIP_FILE` environment override to install.sh that skips the GitHub download and uses the given local zip. Verify: manual run against a locally built zip in a temp dir shows `commands/load-skill.md` and a correct marker; run against a manifest with `releaseVersion` removed still exits 0 with no marker.
- [ ] 1.5 Update `scripts/verify-install.mjs` parity expectations to include the commands deploy and version marker in both installer trees. Verify: `node scripts/verify-install.mjs` exits 0.

## 2. Git zoning migration

- [ ] 2.1 Tag current master `pre-dogfood-zoning` so all tracked mirror copies stay recoverable from history. Verify: `git tag -l pre-dogfood-zoning` lists the tag.
- [ ] 2.2 Create `scripts/generate-gitignore-block.mjs` — implements spec requirement "Gitignore block generated from manifest" and design decision "Gitignore block is generated, not hand-maintained": rewrites the region between `# BEGIN dogfood-mirror (generated)` and `# END dogfood-mirror` in `.gitignore` with entries derived from the manifest (each skill's `.claude/skills/` mirror path, `.claude/hook/`, `.claude/commands/load-skill.md`, `.claude/.dogfood-prev/`, `.claude/.toolset-version`, `dist/release-lkg.zip`, `.agents/discovery.json`, `.agents/ts-deliver-router/state.json`, `.agents/ts-deliver-router/history.jsonl`); never touches content outside the markers; idempotent. Verify: run twice → `.gitignore` byte-identical; pre-existing entries outside the block unchanged.
- [ ] 2.3 Execute spec requirement "Mirror and state paths untracked in git" following design decision "Untrack mirror and state files in ignore-then-untrack order": run the generator, commit `.gitignore`; then `git rm -r --cached` the mirror paths (7 deliverable skill copies under `.claude/skills/`, `.claude/hook/`, `.claude/commands/load-skill.md`) and the three `.agents` state files, keeping disk copies, and commit. Verify: `git ls-files .claude/ | grep -E 'hook/|skills/(ondemand|taco-project-scaffolder|ts-acpl|ts-deliver-router|ts-orchestrate|ts-project-planner|ts-project-scaffolder)'` empty; `git ls-files .claude/skills/spectra-propose` non-empty (dev tooling still tracked); `git status --porcelain` clean.

## 3. Dogfood sync and safety rings

- [ ] 3.1 [P] Create `test-fixtures/hook-payloads/` with one JSON fixture per hook event used by manifest hooks (at minimum a `UserPromptSubmit` payload with `session_id`, `prompt`, `cwd` fields matching Claude Code's hook stdin schema). Verify: `bash src/hook/inject-workflow-state.sh < test-fixtures/hook-payloads/user-prompt-submit.json` exits 0.
- [ ] 3.2 [P] Create `scripts/ring0-check.mjs` — implements spec requirement "Ring 0 static verification gate" and design decision "Ring 0 static gate: skill lint plus hook smoke tests": for each manifest skill assert `SKILL.md` exists and frontmatter parses; for each manifest hook runnable on the current platform, execute it with its fixture payload on stdin and assert exit 0; print a warning for platform-skipped hooks; exit non-zero naming the first failing artifact. Verify: clean run exits 0; planting `exit 1` in a copy of a hook makes it exit non-zero naming that hook.
- [ ] 3.3 Create `scripts/dogfood.mjs` — implements spec requirements "Dogfood mirror sync is manifest-driven with mirror semantics" and "Pre-sync snapshot and rollback", per design decision "Mirror sync uses delete-then-copy with pre-sync snapshot" — with three modes: default sync (run ring0-check, snapshot current mirror paths to `.claude/.dogfood-prev/`, then per manifest entry delete the `.claude/` mirror path and copy from `src/` through the exclusion filter; refuse any computed target outside `.claude/`), `--rollback` (restore mirror from `.claude/.dogfood-prev/`, non-zero if no snapshot), and `--from-zip <path>` (sync mirror from a zip's contents instead of `src/`, used by restore-lkg). Verify: after sync `git status --porcelain` empty; plant stale file in mirror → gone after sync; rollback restores pre-sync bytes; rollback with no snapshot exits non-zero with reason.
- [ ] 3.4 Wire npm scripts in `package.json` — covers spec requirement "Last-known-good zip recovery" and design decision "Last-known-good zip for nuclear recovery": `dogfood` (ring0 + sync), `dogfood:rollback`, `dogfood:bless` (copy `dist/release.zip` → `dist/release-lkg.zip`), `dogfood:restore-lkg` (`dogfood.mjs --from-zip dist/release-lkg.zip`, non-zero with reason when LKG missing), `ring0`, `pilot`. Verify: `npm run` lists all six; `npm run dogfood:restore-lkg` without LKG exits non-zero with printed reason.
- [ ] 3.5 Create `scripts/pilot.mjs` — implements spec requirement "Ring 1 pilot install in disposable fixture" and design decision "Ring 1 pilot install runs the real installer against a fixture": build the zip, create a temp fixture project dir, run `release/install.sh` with `ZIP_FILE` pointing at the local zip, assert installed tree (manifest skills under `.claude/skills/`, project-scoped hooks in `.claude/hooks/`, `commands/load-skill.md`, `.claude/.toolset-version` matching `package.json`), run each installed project-scoped hook against its fixture payload asserting exit 0, print PASS/FAIL, exit accordingly, and delete the fixture. Verify: `npm run pilot` prints PASS and exits 0; repo `git status --porcelain` unchanged by the run; deleting a file from the staged zip makes pilot FAIL naming the path.

## 4. End-to-end verification

- [ ] 4.1 Full sequence in the main tree: `npm run release` (or equivalent build), `npm run pilot`, `npm run dogfood`, then `git status --porcelain` → empty; `npm run dogfood:bless` creates `dist/release-lkg.zip`; `npm run dogfood:restore-lkg` re-syncs and git stays clean. Record results in the change notes. Verify: every command exits 0 and the porcelain checks are empty.
- [ ] 4.2 Run `npm run type-check` and `npm test` to confirm no project regressions; update `docs/architecture.md` Build and Distribution section and `USER_GUIDE.md` install notes to document the new commands (`dogfood`, `pilot`, `dogfood:rollback`, `dogfood:bless`, `dogfood:restore-lkg`) and the version marker. Verify: type-check and tests exit 0; docs mention each new command exactly once in the relevant section.
