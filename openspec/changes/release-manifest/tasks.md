## 1. Manifest Schema and Generator

- [x] 1.1 Create `scripts/release-manifest.json` using manifest format: flat arrays of typed entries (design §Manifest format). The manifest is valid JSON parseable by python3 and PowerShell — standard JSON, no comments, no trailing commas. Fields: `version: "1"`, `skills` array (one entry per directory under `src/skills/` and `src/skills/ondemand/`), `hooks` array (one entry per file in `src/hook/`). Verify: `python3 -c "import json; m=json.load(open('scripts/release-manifest.json')); assert m['version']=='1'; assert any(e['dest']=='skills/ondemand/ts-project-init-advisor' for e in m['skills'])"` exits 0 and PowerShell `Get-Content scripts/release-manifest.json | ConvertFrom-Json` succeeds.

- [x] 1.2 Implement `scripts/generate-manifest.mjs` using generator strategy: scan-then-merge (design §Generator strategy). Generator scans src and produces scripts/release-manifest.json by scanning `src/skills/` (including `ondemand/`) and `src/hook/`. Generator assigns default scope to hook entries: `inject-workflow-state.sh` → `"project"`, all others → `"user"`. Generator preserves existing scope values on re-run by merging with any existing manifest. Exit non-zero with error message if `src/skills/` or `src/hook/` is missing (spec: manifest-generator §Generator exits non-zero on missing source directories). Verify: run generator twice; assert manifest unchanged; assert `inject-workflow-state.sh` has `scope: "project"`; assert a removed hook disappears.

- [x] 1.3 Update `package.json` `release` script to `"node scripts/generate-manifest.mjs && node scripts/build-release.mjs"` so that `npm run release` chains generator before build (spec: manifest-generator §npm run release chains generate-manifest before build-release). Verify: `npm run release` succeeds; `dist/release.zip` is produced; `scripts/release-manifest.json` reflects current `src/` state.

## 2. Build Script Refactor

- [x] 2.1 Implement build-release.mjs: read manifest, remove skill enumeration loop (design §build-release.mjs). Refactor `scripts/build-release.mjs` so build-release.mjs reads manifest instead of hardcoding skill enumeration — remove `copySkillsDir` recursive function and hardcoded hook allowlist, replace with iteration over `manifest.skills` and `manifest.hooks`. Skills copied to `BUILD/<entry.dest>`; hooks to `BUILD/<entry.dest>`. Manifest absence → non-zero exit with `"scripts/release-manifest.json not found — run generate-manifest first"`. Verify: delete `scripts/release-manifest.json`, confirm non-zero exit with correct message; restore manifest, run `npm run release`, inspect zip for all skills including `ondemand/ts-project-init-advisor`.

- [x] 2.2 Embed `scripts/release-manifest.json` as `manifest.json` at the zip root during the build step (spec: release-manifest §Manifest file declares all release artifacts). This file is what install scripts read after extracting the zip. Verify: unzip `dist/release.zip`, confirm `manifest.json` exists at root and parses cleanly with `python3 -c "import json; json.load(open('manifest.json'))"`.

## 3. Install Script Refactor

- [x] 3.1 Implement install.sh: replace skill/hook loops with manifest-driven loops (design §install.sh). Parse `manifest.json` from extracted zip using `python3`; install scripts route artifacts via manifest fields — iterate `manifest.skills` (copy by `dest` path) and `manifest.hooks` (route by `scope`: `user` → `$HOOKS_DIR`, `project` → `$PROJECT_HOOKS_DIR`). Implement backward compatibility when manifest absent from zip: if `manifest.json` not in zip, fall back to current logic. Verify: extract zip, run install script, confirm `ts-project-init-advisor` lands at `[project]/.claude/skills/ondemand/ts-project-init-advisor/` and `inject-workflow-state.sh` at `[project]/.claude/hooks/`; verify old zip without manifest still installs without error.

- [x] 3.2 Implement install.ps1: same logic via ConvertFrom-Json (design §install.ps1). Parse `manifest.json` using PowerShell `ConvertFrom-Json`; install scripts route artifacts via manifest fields — same routing as task 3.1. Backward compatibility when manifest absent from zip: fall back to current logic if no manifest. Verify: on a system with `pwsh`, extract zip, run install.ps1, confirm matching layout to install.sh result.

## 4. Parity Check and CI

- [x] 4.1 Implement verify-install.mjs: mock-zip structural parity check (design §verify-install.mjs). Script satisfies verify-install.mjs requires bash and pwsh on CI runner: checks both at startup, exits with `"pwsh not found — required to verify install.ps1"` (or bash equivalent) if missing. Satisfies verify-install.mjs asserts install.sh and install.ps1 produce identical directory trees: builds mock dir matching manifest zip layout, runs both scripts in isolated temp dirs, compares file trees, exits non-zero with diff if trees differ. Verify: `node scripts/verify-install.mjs` exits 0 after tasks 3.1 and 3.2 complete; confirm early exit message when pwsh unavailable.

- [x] 4.2 Add a `verify-install` step to `.github/workflows/test.yml` that runs after `npm run release` and executes `node scripts/verify-install.mjs`. CI job SHALL fail if verify-install exits non-zero (spec: install-parity-check §verify-install.mjs is wired into CI). Verify: introduce a deliberate divergence in install.sh (copy a skill to wrong dest), run CI locally with `act` or push to branch, confirm the verify-install step fails.
