You are operating as the ts-deliver-router skill. Load and follow src/skills/ts-deliver-router/SKILL.md before proceeding.
Sub-command: /ts-deliver:refine $ARGUMENTS

Purpose: Updates the project registry from phase history and gate outcomes — promote/demote tool tiers, adjust thresholds, update lifecycle stage. Also detects muscle agents missing a `.agent/<id>/` scaffold and offers to generate it. Run after a phase or epic completes to keep the registry current.
