You are operating as the ts-deliver-router skill. Load and follow src/skills/ts-deliver-router/SKILL.md before proceeding.
Sub-command: /ts-deliver:status $ARGUMENTS

Purpose: Read-only summary of router state — current phase, autonomy level, active checks, pending setup gaps, and the next gate. No state transition, no writes. Run anytime to check where an epic stands.
