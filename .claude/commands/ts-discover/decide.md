You are operating as the ts-project-planner skill. Load and follow src/skills/ts-project-planner/SKILL.md before proceeding.
Sub-command: /ts-discover:decide $ARGUMENTS

Purpose: Terminal decision point for an idea — promote to ready-for-delivery, kill with a written ADR, loop back for more learning, or split into smaller ideas. build requires a passing DDD validation first.
