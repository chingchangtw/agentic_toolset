You are operating as the ts-project-planner skill. Load and follow src/skills/ts-project-planner/SKILL.md before proceeding.
Sub-command: /ts-discover:idea $ARGUMENTS

Purpose: Seeds the Discovery backlog with one new candidate (status=idea). The cheapest entry point for capturing a raw idea before any exploration work begins. Also used internally as feedback intake from ts-deliver-router.
